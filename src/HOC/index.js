import React from 'react'

const color = (WrappedComponent) => {
    const colorList = [red, green];
    return(props) => {
        return (
            <div className={`color`}>
                
            </div>
        )
    }
    
}
export default color;
